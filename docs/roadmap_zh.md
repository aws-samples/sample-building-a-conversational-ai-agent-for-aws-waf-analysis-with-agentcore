# 路线图

[English](roadmap.md) | 中文

我们计划支持什么、修复什么。

**右侧有日期表示已经发布。** 没有日期表示还没做，也不构成承诺。每组里没有日期的条目大致按我们打算动手的顺序排列，顺序可能会变。

## 日志查询与分区

| | |
|---|---|
| 桶内前缀格式中途变过的情况下，正确识别分钟级分区 | |
| 桶里同时存在两种分区格式时，告诉你从哪一天起可以做分钟级日志查询 | |
| 支持分区列不叫 `log_time` 的现有 Athena 表 | |
| 读取你自己转换好的 Parquet 格式 WAF 日志表 | |
| 在对话里解释小时级分区的限制，以及那次一劳永逸的 Firehose 改动 | 2026-06-24 |
| 复用现有 Athena 表而不是新建，表 location 失效时自动自愈 | 2026-06-06 |
| 多个 WebACL 共用一个 S3 前缀时，互相不串数据 | 2026-06-06 |

## 长时间查询

| | |
|---|---|
| 大查询运行期间保持连接，并显示扫描进度 | |
| 问题所需的扫描范围超出单轮能力时，直说，并给出该换成什么问法 | |
| 停止按钮真正取消 Athena 查询，而不只是断开浏览器请求 | |
| 报告里某段被跳过时明确说出来，而不是留一片空白 | |

## 分析能力

| | |
|---|---|
| 一个通用的日志聚合查询，任意受支持的过滤条件搭配任意受支持的分组维度 | |
| 规则级和端点级命中率，包括 Count 切 Block 之后的误报率 | |
| 引导式 SQL 注入调查，和现有的误报调查、绕过调查一样成体系 | |
| 识别同一个 WAF token 在大量 IP 上被复用 | |
| 国家、referer、网络（ASN）集中度作为补充维度 | |
| 用域名找到对应的 WebACL，不再反问你是哪一个 | |
| 巡检报告里跨 WebACL 的汇总 | |
| 识别同一个 TLS 指纹背后轮换 User-Agent 的行为 | 2026-06-24 |
| 按 token 失败原因拆解真实用户为什么过不了 Challenge / CAPTCHA | 2026-06-24 |
| 给出用 CloudWatch 告警监控规则拦截率和误报率的做法 | 2026-06-24 |
| 每周安全巡检报告，含图表和按规则的请求内容 | 2026-06-07 |

## 安全与隐私

| | |
|---|---|
| 日志内容永远当数据看，即使某个日志字段里写着一条指令 | |
| 报告渲染时对所有字段做转义 | |
| 针对提示注入的自动化测试，同时验证攻击载荷仍能以文本形式正常显示 | |
| Agent 输出在浏览器渲染前做净化 | 2026-07-20 |
| 展示请求内容时掩码其中的密钥值 | 2026-06-07 |

## 文档

| | |
|---|---|
| 公开一份已知能力边界清单 | |
| Athena 表检测指南 | 2026-06-05 |
| Firehose 分钟级分区指南 | 2026-05-25 |
| 使用指南、IAM 权限、成本估算 | 2026-05-11 |
